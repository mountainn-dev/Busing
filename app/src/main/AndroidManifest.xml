<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_busing"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_busing"
        android:supportsRtl="true"
        android:theme="@style/Theme.Busing" >
        <activity
            android:name=".view.screen.station.StationDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.route.RouteInfoActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.route.RouteDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.screen.HomeActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>